# 彩心堂 現場管理アプリ — 開発仕様書

## 2026-09-08：ホームとスマホカレンダーのUI・UX改修

- ホームは白・ティール・淡いグリーンの配色に統一。今日／明日の切替、1週間の日付選択、稼働予定・配置人数・出面未確認のサマリー、現場別カードを追加。予定のない現場は折りたたみから開く。
- 明日は配置予定、今日・過去日は出面の編集シートに接続。ホームから未来の出面が作られないよう旧入力関数も保護。チップのタップで状態を循環変更するUIを、名前と状態を読んで編集する明示操作に置き換えた。
- 月の日付タップは画面下の予定プレビューを更新。プレビュー・工程バー・担当者から日別シートを開く。「ほか○件」は選択日の全現場を日表示で一覧する。
- 日・週・月・リストの4表示。リストは選択日から30日間、次の30日へ移動できる。週は7日の2列グリッドで、複数現場は1件と「ほか○件」を表示する。
- 今日、表示切替、追加を画面下部に固定。月の左右スワイプに短い移動アニメーションを追加し、端末の視差効果軽減設定を尊重。日付ボタンは矢印キーでも移動できる。
- カレンダーの文字・罫線・週末色・今日と選択日の色・シートを統一。工程バーは保存済みの先頭工程色を現場の識別色に使う。保存済みID・日別記録・工程データの移行は行わない。
- PWAのテーマ色とキャッシュを更新。同期・バックアップの説明は表示設定に集約。

### 対応範囲と検証

FirstSeedの公式[製品説明](https://firstseed.io/jp/calendar/calendar.html)と[予定操作ガイド](https://firstseed.io/calendar/calendar-guide-events.html)を参照。月を中心にした片手操作、予定確認、表示切替、追加・編集・複製などの操作を現場管理のデータに合わせて実装。完全な同一機能ではなく、時間軸型の日／週表示、連続月の縦スクロール、自然言語入力、Appleカレンダー・リマインダー同期、ウィジェットは未実装。

`tests/calendar.test.cjs` のブラウザー回帰テストを14項目に拡張。既存11項目に加えて、日付プレビュー・リスト移動、ホームの予定／実績分離、320／390／430／1024pxでの横はみ出し・固定操作の重なりを検証。架空データを用いた画面画像は `artifacts/ui-review/` に生成。Chromeで確認し、実機iPhone／Safariは未確認。

実行例（このMacの同梱環境）：

```sh
NODE_PATH=/Users/hiroshitakashima/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules node --test tests/calendar.test.cjs
```

---

## 2026-09-07：FirstSeed Calendarを参考にしたカレンダー実装

以下は9月7日時点でPWA本体 `genba-kanri.html` に実装した内容。9月8日の改修については上の節を優先し、古い本文と異なる場合は日付の新しい節を現状とする。

- 起動時は全現場の月カレンダー。大きな年月、土日の色分け、今日の丸表示、選択日の枠、下部の日／週／月切替と追加ボタン。
- 現場ごとの工程期間をまとめ、週境界をまたぐ青いバーとして表示。途切れた工程の空白期間は勝手につなげない。工程の外の日別記録も該当日に表示する。
- バーの直下に、その現場・日付の配置または出面を人名チップで表示。宏＝黄、隆一＝緑、昇＝灰。既存の作業者ID・保存色は変更しない。
- 左右スワイプ／前後ボタン、今日への移動、日付指定、現場絞り込み、現場・工程・住所検索、文字サイズ、表示件数設定。
- 週表示は左4日・右3日のグリッド。現場が多い日は「ほか○件」から詳細へ。日表示は現場と担当者の一覧。
- 日付・配置をタップすると日別シート。現場を切り替え、終日／半日予定、出勤／半日／欠勤、仕事内容、休工を編集する。入力は既存の端末保存へ反映する。
- 「予定どおりの出面を確定」は明示操作で実績を作る。日曜や祝日でも、日付経過だけでは出面・休工を確定しない。未来の実績は入力できない。
- 予定・工程の追加、編集、複製、削除。任意で曜日と担当者を指定し、期間内の未配置の日だけへまとめて配置する。既存の例外・休工・実績を保持する。
- PCでは、単一工程に対応するバーをドラッグして期間を移動できる。確認時に「配置・出面は元の日付に残る」と示す。複数工程をまとめたバーは日別詳細から個々の工程を編集する。
- スマートフォンでは日付の長押しから新規予定を開ける。期間の変更・複製は編集シートから操作する。
- 日別詳細から既存の出面PDF／CSVに進める。表示設定から全現場データのJSONを手動保存できる。JSON復元画面・自動バックアップは未実装。
- 空の端末で架空の現場・出面を自動生成する処理を廃止。既に保存されたデータは削除しない。新規現場の初期メンバーは会社の3人。
- 月バー直下の日別配置を正確に対応させるため、月表示は独自のCSSグリッドに変更。FullCalendarのCDN読込を外し、単一HTMLとPWAの構成を維持。サービスワーカーのキャッシュ版を更新。

### 互換性と未接続の範囲

既存の `genba_sites`、現場ID、工程ID、作業者ID、`workerStatus`、`memo` を継続使用する。追加項目は `planWorkers` の `half`、`planState`／`actualState` の `rest`、意図的な配置解除を保つ `planState: unassigned`、`updatedAt`。確定済み出面を、予定の移動や削除から更新しない。PDF／CSVの人工は従来どおり `present=1`、`half=0.5` から計算する。

Google自動同期、端末間の共有、彩速ミツモ／Misoca／RIOとの業務接続はまだ行っていない。画面には「端末保存・Google未接続」と明示する。バックアップボタンは端末内データの手動退避であり、Google同期の代わりでも自動復元機能でもない。React Native／Expo版のファイルは今回変更していない。

### 検証

`tests/calendar.test.cjs` にPlaywrightによるブラウザー回帰テストを追加。独立したブラウザープロファイルと架空データのみで実行する。PlaywrightのあるNode環境から `node --test tests/calendar.test.cjs` を実行する。既定ではインストール済みChromeを使い、別バイナリは `CALENDAR_BROWSER_PATH` で指定できる。

初期表示での既存データ保持、保存後の再読込、予定と実績の分離、日曜施工・半日・休工、未来の実績抑止、曜日別一括配置と例外保持、予定の編集・複製・削除、検索、6週ある月、月境界、CSVとJSON退避、空の初回起動、名前のHTMLエスケープ、現場多数、月スワイプとドラッグを検証する。iPhone相当390px、狭幅320px、PC1024pxで表示と操作を確認。実機iPhone／Safariでの最終確認は別途必要。

参照：FirstSeed公式の[予定操作](https://firstseed.io/jp/calendar-guide-events.html)・[表示設定](https://firstseed.io/jp/calendar/calendar-guide-settings.html)。予定の見せ方と操作の参考にし、製品名・ロゴ・専用素材は流用していない。9月22日の国民の休日と5月の連続祝日後の振替休日を[内閣府の2026年公表](https://www8.cao.go.jp/chosei/shukujitsu/gaiyou.html)に照合して補正した。その他の年の祝日計算は既存の近似式を継続しており、過去の特例・将来の変更をすべて網羅するものではない。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| **プロジェクト名** | 彩心堂 現場管理（出面管理システム） |
| **オーナー** | 株式会社彩心堂 |
| **業種** | 内装施工業 |
| **利用者** | 彩心堂 社員 3名（全員同じ権限） |
| **対象端末** | iPhone / Google Pixel / iPad / PC |
| **本番URL** | https://ts09design.github.io/genba-kanri/genba-kanri.html |
| **リポジトリ** | https://github.com/ts09design/genba-kanri |

---

## 2. アプリの目的

**紙の出面表をデジタル化する。**

内装施工現場における「誰が何日出たか」を記録・集計し、月末にA4 PDFとして出力できるアプリ。
将来的には3台の端末でリアルタイムにデータを共有することを目指す。

---

## 3. 現在の技術スタック

| 要素 | 技術 |
|------|------|
| **フロントエンド** | 単一HTMLファイル（Vanilla JS, インラインCSS） |
| **フレームワーク** | なし（依存ライブラリゼロ） |
| **データ保存** | localStorage（端末ローカル） |
| **配信** | GitHub Pages（静的ホスティング, 無料） |
| **PWA** | manifest.json + Service Worker（オフラインキャッシュ） |
| **PDF出力** | HTML生成 → window.print() |

**ファイル構成:**
```
genba-kanri/
├── genba-kanri.html    ← メインアプリ（全機能, 約1740行）
├── manifest.json       ← PWAマニフェスト
├── sw.js              ← Service Worker
├── assets/
│   ├── apple-touch-icon.png (180x180)
│   ├── icon-512.png (512x512)
│   └── genba_icon_a.PNG (1024x1024 原本)
├── SPECIFICATION.md    ← この文書
│
│  ※ 以下は旧実装（アーカイブ予定）
├── genba-kanri-app.jsx ← React版プロトタイプ（未使用）
├── App.js             ← Expo版エントリ（未使用）
├── src/               ← Expo版ソース（未使用）
└── package.json       ← Expo依存（未使用）
```

---

## 4. 実装済み機能

### 4.1 現場管理
- 現場の作成・編集・削除
- 現場情報: 現場名, 住所(Googleマップリンク), 元請会社, 元請担当者, 自社担当者, 電話番号(タップ発信), 備考
- 現場一覧: 検索, メンバー数・今月人工の表示

### 4.2 メンバー管理
- メンバーの追加・編集・削除
- カスタムアイコン文字・カラー設定
- ★主要メンバー機能（星トグルで上位ソート）
- **デフォルト登録**: 新規現場に彩心堂4名が自動追加
  - 髙嶋 宏（宏/赤）, 髙嶋 隆一（隆/紫）, 髙嶋 昇（昇/緑）, 佐久間 英二（佐/橙）

### 4.3 カレンダー・出面管理
- 月間カレンダー表示（iOSカレンダー風, 週行+スパンバー）
- 工程イベント: 複数日にまたがる色付きバー（タイトル表示）
- 工程リスト: カレンダー下に当月の工程一覧
- 日ごとの入力:
  - **予定タブ**: 予定人数入力 + メンバー追加（複数同時選択）
  - **実績タブ**: メンバー追加 → 出勤/半日/欠勤の状態切替
  - 「★主要メンバー全員」ワンタップ追加ボタン
  - 工程予定メモ / 作業メモ
- カレンダーセル: 日付(右上) + 工程バー + 予定人数(青文字) or 実績人数(赤丸) + メモドット(橙)
- 日詳細パネル: 左右矢印ナビ + スワイプ対応
- 月ナビ: 前月/翌月 + 今日ボタン

### 4.4 PDF出力
- A4横1枚のレイアウト
- 実績のみ（予定情報は含まない）
- メンバー × 日のマトリクス表（○/△）
- 右パネルにサマリー（月間合計人工 + メンバー別集計）
- ヘッダー: 現場名, 元請会社, 元請担当者, 電話番号, 住所

### 4.5 PWA
- ホーム画面追加でアプリとして動作
- Service Workerによるオフラインキャッシュ
- カスタムアイコン

---

## 5. データモデル

```javascript
// localStorage キー: "genba_sites"
// 値: Site[] (JSON)

Site = {
  id: string,
  name: string,              // 現場名
  address: string,           // 住所
  generalContractor: string, // 元請会社
  gcManager: string,         // 元請担当者
  manager: string,           // 自社担当者
  phone: string,             // 電話番号
  notes: string,             // 備考
  workers: Worker[],         // 登録メンバー
  dayLogs: { [dateKey]: DayLog }, // 日次ログ (dateKey = "YYYY-MM-DD")
  events: Event[],           // 工程イベント
  createdAt: string,         // ISO日時
}

Worker = {
  id: string,
  name: string,
  color: string,     // HEXカラー
  icon: string,      // アイコン文字（空=名前の頭文字）
  starred: boolean,  // 主要メンバーフラグ
}

DayLog = {
  workerStatus: { [workerId]: "present" | "half" | "absent" | "" },
  planWorkers: { [workerId]: "planned" | "" },
  memo: string,              // 作業メモ（実績）
  planMemo: string,          // 工程予定メモ
  plannedHeadcount: number,  // 予定人数
}

Event = {
  id: string,
  title: string,     // 工程名
  start: string,     // 開始日 "YYYY-MM-DD"
  end: string,       // 終了日 "YYYY-MM-DD"
  color: string,     // バー色（6色固定パレット）
}
```

---

## 6. 開発ロードマップ（未実装）

### Phase 1: Firebase Firestore によるデータ共有
**最重要。3人で同じデータをリアルタイム共有する。**

- Firebase Firestore Spark プラン（無料枠）を使用
- データ構造:
  ```
  /teams/{teamId}/sites/{siteId}
  /teams/{teamId}/sites/{siteId}/dayLogs/{dateKey}
  /teams/{teamId}/sites/{siteId}/events/{eventId}
  ```
- リアルタイム同期: `onSnapshot` リスナー
- 簡易アクセス制御: チームID（URL共有）
- localStorage → Firestore への読み書き切替

### Phase 2: オフライン対応 + UI改善
- オフライン時はlocalStorageに書き込み → オンライン復帰時にFirestoreへ同期
- 同期状態インジケーター
- 週間表示（メンバー×日のマトリクス）
- 「今日」ボタンから即入力画面へ

### Phase 3: PDF・レポート強化
- 週間表示のPDF出力
- 作業員別月間集計レポート
- 複数月比較
- 会社ロゴ対応

---

## 7. 重要な制約・方針

| 方針 | 理由 |
|------|------|
| **ランニングコストは実質無料** | GitHub Pages(無料) + Firebase Spark(無料枠)。有料化する可能性があるなら導入しない |
| **PWAで配信** | App Store不要。3人の社内ツールには十分 |
| **単一HTMLファイル** | 依存ライブラリゼロ。保守性・可搬性が最優先 |
| **3人全員同じ権限** | 管理者/一般ユーザーの区別なし |
| **UIはスマホファースト** | 現場でiPhoneから使うのがメイン。PCは補助 |
| **カレンダーUIの視認性** | 日付が常に見える。数字の氾濫を避ける。工程バーはiOSカレンダー風 |

---

## 8. デフォルトメンバー定数

```javascript
const DEFAULT_WORKERS = [
  { id: "saishindo-1", name: "髙嶋 宏",     color: "#DC2626", icon: "宏",  starred: true },
  { id: "saishindo-2", name: "髙嶋 隆一",   color: "#7C3AED", icon: "隆",  starred: true },
  { id: "saishindo-3", name: "髙嶋 昇",     color: "#059669", icon: "昇",  starred: true },
  { id: "saishindo-4", name: "佐久間 英二", color: "#D97706", icon: "佐",  starred: true },
];
```

---

## 9. 開発環境

- **OS**: macOS (Apple Silicon)
- **Node.js**: v20.20.2 (nvm管理)
- **デプロイ**: `git push` → GitHub Pages 自動反映（1-2分）
- **テスト**: ブラウザでの手動確認（自動テストなし）

---

## 10. 開発者への期待

1. **Firebase Firestore の導入経験**があること
2. **PWA / Service Worker** の実務経験
3. **モバイルファーストUI** の設計能力
4. Vanilla JS での開発に抵抗がないこと（React等への移行は検討可だが、シンプルさを優先）
5. 彩心堂のオーナーと直接コミュニケーションし、要望をアプリに反映できること
